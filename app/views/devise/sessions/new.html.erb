<% content_for :navbar do %>
<% render "layouts/navbar" #render navbar partials here%>
<% end %>

<h2>Log In</h2>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div><%= f.label :email %>
  <%= f.email_field :email %></div>

  <div><%= f.label :password %>
  <%= f.password_field :password %></div>

  <% if devise_mapping.rememberable? -%>
    <label class="checkbox">
      <input type="checkbox"> Remember Me
    </label>
  <% end -%>

  <br/>
  <button type="submit" class="btn btn-danger">Log In</button>
<% end %>


<div id="login_links">
  <%= render "devise/shared/links" %>
</div>

<br/>
<p><b>Don't want to sign up?</b>
<br/>We won't be able to save your preferences and improve our recommendations if you choose this option.</p>
<a href="/" class="btn">I do not wish to sign up</a>
