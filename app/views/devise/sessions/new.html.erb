<% content_for :navbar do %>
<% render "layouts/navbar" #render navbar partials here%>
<% end %>

<!--FIX-ME: move it somewhere else, front-end fix -->
<a href="/users/sign_up" class="btn btn-danger">Sign Up Now</a>
<a href="/" class="btn btn-danger">I do not wish to sign up</a>

<h2>Log In</h2>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div><%= f.label :email %>
  <%= f.email_field :email %></div>

  <div><%= f.label :password %>
  <%= f.password_field :password %></div>

  <% if devise_mapping.rememberable? -%>
  	<label class="checkbox">
      <input type="checkbox"> Remember Me
    </label>
  <% end -%>

  <br/>
  <button type="submit" class="btn btn-danger">Log In</button>
<% end %>

<%= render "devise/shared/links" %>
